PORT=5000
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/securevault?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
VAULT_ENCRYPTION_KEY=your-32-character-encryption-key-change-this
NODE_ENV=development

# Single-service Render deploy: frontend + backend share the same origin, so CORS_ORIGIN is optional.
# If you later split frontend/backend or host the frontend elsewhere, set:
# CORS_ORIGIN=https://your-frontend-domain
CORS_ORIGIN=http://localhost:5173